# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Template' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for Template
  pod 'YYWebImage',:inhibit_warnings => true
  pod 'QMUIKit',:inhibit_warnings => true
  pod 'MFNetworkManager',:inhibit_warnings => true
  pod 'SAMKeychain',:inhibit_warnings => true
  pod 'MJRefresh',:inhibit_warnings => true
  pod 'MBProgressHUD',:inhibit_warnings => true
  pod 'YTKNetwork',:inhibit_warnings => true
  pod 'YYModel',:inhibit_warnings => true
  pod 'Bugly',:inhibit_warnings => true
  pod 'Texture',:inhibit_warnings => true
  pod 'DateTools',:inhibit_warnings => true
  pod 'JPush',:inhibit_warnings => true
  pod 'FreeStreamer', '~> 3.8.2',:inhibit_warnings => true
  pod 'CocoaLumberjack', '~> 3.0.0'
  pod 'SSZipArchive'
  pod 'Masonry'
  pod 'MFExtractColor'
end
